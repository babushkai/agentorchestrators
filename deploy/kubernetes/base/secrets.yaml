apiVersion: v1
kind: Secret
metadata:
  name: orchestrator-secrets
  namespace: agent-orchestrator
  labels:
    app.kubernetes.io/name: agent-orchestrator
type: Opaque
stringData:
  DATABASE_USER: "orchestrator"
  DATABASE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  # Set these in your actual deployment
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""
